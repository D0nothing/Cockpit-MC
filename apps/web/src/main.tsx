import React, { useEffect, useMemo, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { Activity, ArrowLeft, ArrowUpRight, BookOpen, Boxes, Check, CheckCircle2, ChevronRight, Circle, CircleAlert, Clock3, ExternalLink, FileText, Fingerprint, GitPullRequestDraft, Github, LayoutDashboard, Link2, MoreHorizontal, Search, Settings, ShieldAlert, ShieldCheck, Sparkles, TicketCheck, Tickets, UserRound } from 'lucide-react';
import type { TicketSummary } from '@vistory/contracts';
import { demoTickets, getTicket, getTickets } from '../lib/data';
import '../app/globals.css';

const labels: Record<string, string> = { imported: 'Importé', assigned: 'Assigné', spec_review_required: 'Spec à valider', second_validation_required: 'Double validation', ready_for_ai: 'Prêt pour IA', ai_requested: 'IA demandée', ai_running: 'IA en cours', ci_running: 'CI en cours', human_review_required: 'Revue humaine', done: 'Terminé', blocked: 'Bloqué' };
const tones: Record<string, string> = { spec_review_required: 'amber', second_validation_required: 'purple', ci_running: 'blue', assigned: 'neutral', done: 'green' };
const navigation = [[LayoutDashboard, 'Vue d’ensemble', '/'], [Tickets, 'Tickets', '/'], [FileText, 'Spécifications', '/'], [Activity, 'Workflows IA', '/'], [ShieldCheck, 'Journal d’audit', '/audit'], [Boxes, 'Projets', '/']] as const;

function App() {
  const path = window.location.pathname;
  const page = path.startsWith('/audit') ? <AuditPage /> : path.startsWith('/tickets/') ? <TicketDetail id={decodeURIComponent(path.split('/').at(-1) ?? '')} /> : <Dashboard />;
  return <div className="shell"><aside className="sidebar"><a href="/" className="brand"><span className="brand-mark">V</span><span>Vistory <b>OS</b></span></a><nav>{navigation.map(([Icon, label, href], index) => <a href={href} className={index === 0 ? 'active' : ''} key={label}><Icon size={18}/><span>{label}</span>{label === 'Workflows IA' && <i>3</i>}</a>)}</nav><div className="sidebar-bottom"><a href="/"><BookOpen size={18}/>Documentation</a><a href="/"><Settings size={18}/>Paramètres</a><div className="profile"><span>AM</span><div><strong>Alice Martin</strong><small>Responsable projet</small></div></div></div></aside><main>{page}</main></div>;
}

function Dashboard() {
  const [tickets, setTickets] = useState<TicketSummary[]>(demoTickets);
  useEffect(() => { void getTickets().then(setTickets); }, []);
  return <div className="page"><header className="topbar"><div><span className="eyebrow">Mardi 23 juin</span><h1>Bonjour Alice <span>👋</span></h1><p>Voici ce qui mérite votre attention aujourd’hui.</p></div><div className="top-actions"><button className="icon-button"><Search size={19}/></button><button className="primary"><Sparkles size={17}/>Nouveau ticket</button></div></header><section className="stats"><article><span className="stat-icon orange"><TicketCheck/></span><div><small>Tickets actifs</small><strong>24</strong><em>+3 cette semaine</em></div></article><article><span className="stat-icon purple"><CircleAlert/></span><div><small>À valider</small><strong>7</strong><em>2 critiques</em></div></article><article><span className="stat-icon blue"><Sparkles/></span><div><small>Workflows IA</small><strong>3</strong><em>en cours</em></div></article><article><span className="stat-icon green"><GitPullRequestDraft/></span><div><small>PR en revue</small><strong>5</strong><em>2 CI validées</em></div></article></section><section className="attention"><div className="section-title"><div><h2>À traiter en priorité</h2><p>Actions qui nécessitent une décision humaine</p></div><button>Tout voir <ArrowUpRight size={16}/></button></div><div className="attention-grid"><article className="attention-card critical"><div className="card-top"><span>Critique</span><small>#142</small></div><h3>Rotation automatique des clés API</h3><p>La spécification est prête. Une seconde validation est requise avant tout lancement IA.</p><div className="card-footer"><div className="avatars"><i>AM</i><i>ML</i></div><a href={`/tickets/${tickets[0]?.id ?? 'demo-142'}`}>Examiner <ArrowUpRight size={15}/></a></div></article><article className="attention-card review"><div className="card-top"><span>Spec à valider</span><small>#139</small></div><h3>Optimiser le chargement du dashboard</h3><p>La spécification générée contient 6 critères d’acceptation et attend votre relecture.</p><div className="card-footer"><div className="due"><Clock3 size={15}/>Il y a 1 h</div><a href={`/tickets/${tickets[1]?.id ?? 'demo-139'}`}>Relire la spec <ArrowUpRight size={15}/></a></div></article></div></section><section className="ticket-section"><div className="section-title"><div><h2>Tickets récents</h2><p>Synchronisés avec GitHub Issues</p></div><div className="filters"><button className="selected">Tous</button><button>Assignés à moi</button><button>À valider</button></div></div><div className="table-wrap"><table><thead><tr><th>Ticket</th><th>Projet</th><th>Statut</th><th>Risque</th><th>Assigné à</th><th>Mise à jour</th><th></th></tr></thead><tbody>{tickets.map(ticket => <tr key={ticket.id}><td><a className="ticket-link" href={`/tickets/${ticket.id}`}><small>#{ticket.externalId}</small><strong>{ticket.title}</strong><span>{ticket.labels.map(label => <i key={label}>{label}</i>)}</span></a></td><td><span className="repo-dot"/> {ticket.repository}</td><td><span className={`status ${tones[ticket.status] ?? 'neutral'}`}>{ticket.status === 'done' && <CheckCircle2 size={13}/>} {labels[ticket.status] ?? ticket.status}</span></td><td><span className={`risk ${ticket.riskLevel}`}>{ticket.riskLevel === 'standard' ? 'Standard' : ticket.riskLevel === 'sensitive' ? 'Sensible' : 'Critique'}</span></td><td><span className="person">{ticket.assignee?.name.slice(0, 2).toUpperCase()}</span>{ticket.assignee?.name}</td><td className="muted">{relativeHour(ticket.updatedAt)}</td><td><MoreHorizontal size={18}/></td></tr>)}</tbody></table></div></section></div>;
}

function TicketDetail({ id }: { id: string }) {
  const [ticket, setTicket] = useState<any>(demoTickets.find((item) => item.id === id) ?? demoTickets[0]);
  useEffect(() => { void getTicket(id).then(setTicket); }, [id]);
  const spec = useMemo(() => ticket.specification?.content ?? `# Objectif\n\n${ticket.description}\n\n## Critères d’acceptation\n\n- La fonctionnalité est couverte par des tests automatisés\n- Aucun secret n’est exposé au navigateur\n- Le changement peut être déployé et annulé sans interruption\n\n## Hors périmètre\n\n- Merge automatique\n- Modification des droits de production`, [ticket]);
  return <div className="page detail-page"><a href="/" className="back"><ArrowLeft size={16}/>Retour aux tickets</a><header className="detail-header"><div><div className="detail-meta"><span>#{ticket.externalId}</span><span className={`risk ${ticket.riskLevel}`}>{ticket.riskLevel}</span><span>{ticket.repository ?? 'vistory-core'}</span></div><h1>{ticket.title}</h1><p>{ticket.description}</p></div><a className="secondary" href={ticket.sourceUrl ?? '#'}><Github size={17}/>Voir sur GitHub<ExternalLink size={14}/></a></header><div className="detail-grid"><div className="detail-main"><section className="panel workflow-panel"><div className="panel-title"><div><Sparkles size={19}/><h2>Progression</h2></div><span>4 / 7 étapes</span></div><div className="steps">{['Ticket importé', 'Contexte consolidé', 'Spécification générée', 'Validation principale', 'Seconde validation', 'Exécution Codex', 'Revue humaine'].map((step, i) => <div className={i < 4 ? 'complete' : i === 4 ? 'current' : ''} key={step}><span>{i < 4 ? <Check size={14}/> : <Circle size={12}/>}</span><small>{step}</small>{i < 6 && <i/>}</div>)}</div></section><section className="panel spec-panel"><div className="panel-title"><div><FileText size={19}/><h2>Spécification technique</h2><span className="version">v{ticket.specification?.version ?? 1}</span></div><button>Modifier</button></div><div className="markdown">{spec.split('\n').map((line: string, index: number) => line.startsWith('# ') ? <h2 key={index}>{line.slice(2)}</h2> : line.startsWith('## ') ? <h3 key={index}>{line.slice(3)}</h3> : line.startsWith('- ') ? <p className="bullet" key={index}>✓ {line.slice(2)}</p> : line ? <p key={index}>{line}</p> : null)}</div></section></div><aside className="detail-side"><section className="panel decision"><div className="decision-head"><ShieldAlert size={21}/><div><h3>Validation requise</h3><p>Ticket classé {ticket.riskLevel}</p></div></div><p>Une seconde personne doit approuver cette spécification avant que Codex puisse être lancé.</p><div className="validator"><span>AM</span><div><strong>Alice Martin</strong><small>Validation principale · approuvée</small></div><Check size={17}/></div><div className="validator pending"><span>ML</span><div><strong>Marc Leroy</strong><small>Validation secondaire · en attente</small></div><Circle size={15}/></div><button className="primary full">Approuver la spécification<ChevronRight size={16}/></button><button className="text-button">Demander des modifications</button></section><section className="panel properties"><h3>Informations</h3><dl><div><dt><UserRound size={15}/>Assigné à</dt><dd>{ticket.assignee?.name ?? 'Non assigné'}</dd></div><div><dt>Risque</dt><dd><span className={`risk ${ticket.riskLevel}`}>{ticket.riskLevel}</span></dd></div><div><dt>Labels</dt><dd>{ticket.labels.map((x: string) => <i key={x}>{x}</i>)}</dd></div><div><dt>Branche IA</dt><dd>Créée après validation</dd></div></dl></section></aside></div></div>;
}

function AuditPage() {
  return <div className="page"><header className="topbar"><div><span className="eyebrow">Intégrité & traçabilité</span><h1>Journal d’audit</h1><p>Chaque événement est chaîné cryptographiquement au précédent.</p></div></header><section className="stats"><article><span className="stat-icon green"><ShieldCheck/></span><div><small>Intégrité</small><strong>100%</strong><em>chaîne vérifiée</em></div></article><article><span className="stat-icon blue"><Fingerprint/></span><div><small>Événements</small><strong>1 284</strong><em>depuis le 1er juin</em></div></article><article><span className="stat-icon purple"><Link2/></span><div><small>Dernier ancrage</small><strong>#42</strong><em>MainChain · il y a 2 h</em></div></article></section><section className="panel audit-explainer"><ShieldCheck size={32}/><h2>Preuves sans exposition des données</h2><p>Le détail reste dans PostgreSQL. Seules les racines Merkle périodiques sont destinées à MainChain, jamais les tickets ou les spécifications en clair.</p></section></div>;
}

function relativeHour(updatedAt: string) {
  return new Intl.RelativeTimeFormat('fr', { numeric: 'auto' }).format(-Math.max(1, Math.round((Date.now() - new Date(updatedAt).getTime()) / 36e5)), 'hour');
}

createRoot(document.getElementById('root')!).render(<React.StrictMode><App /></React.StrictMode>);
